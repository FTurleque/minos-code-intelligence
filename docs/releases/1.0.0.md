# MINOS 1.0.0 — Production Release

Date cible : **1er août 2026**

MINOS 1.0.0 est la première release stable de production du moteur **MINOS Code Intelligence** après la convergence C0→M28 et la promotion qualifiée de `develop` vers `main` via la PR #102.

## Base de production

- PR de promotion : **#102 — MERGED** ;
- `develop` exact-head qualifié avant promotion : `ce4b6ba5f28ecbe3273919318cd950adcf6a0d80` ;
- merge de production initial vers `main` : `71738c1d65cc0aae9fd5c5b34e898d72e164a4f4` ;
- M21-S2 : `PASS_WITH_CONSTRAINTS` — CI recovery PASS, branch protection indisponible sur le plan GitHub privé courant ;
- PR Validation Windows/Linux : PASS ;
- M28 exact-head Windows/Linux : PASS ;
- M19 / M20 / IntelliJ : PASS.

Le tag `v1.0.0` doit pointer sur le commit `main` exact qui contient cette préparation de release. Le workflow de publication vérifie ce SHA avant de créer le tag et la GitHub Release.

## Capacités livrées

MINOS 1.0.0 consolide notamment :

- indexation et snapshots structurés persistants ;
- recherche symbolique, références, dépendances, impact et architecture ;
- plateforme de providers et discovery explicite ;
- provider Java avancé : CFG, def-use, flux interprocéduraux bornés et primitives de sécurité ;
- retrieval sémantique/hybride local optionnel ;
- providers polyglottes C/C++, C#, Go et Rust avec dispositions/capabilities explicites ;
- indexation remote/distribuée sur révisions immuables ;
- observations runtime partielles corrélées au snapshot statique ;
- contrôle tenant / Team-Hosted embarqué et local-first ;
- surfaces CLI, API Java, MCP read-only et plugin IntelliJ ;
- packaging Windows natif avec setup.exe et ZIP portable ;
- SBOM CycloneDX, notices tierces et sidecars SHA-256 ;
- smoke tests d'installation et de désinstallation.

## Limites explicites de 1.0.0

La stabilité 1.0.0 ne transforme pas les limites documentées en capacités implicites :

- le worker remote natif **ne revendique pas une sandbox OS pour code non fiable** ;
- la politique réseau `DENY` reste fail-closed lorsqu'aucun backend OS qualifié ne peut la prouver ;
- le mode Team/Hosted reste une capacité embarquée/local-first et **ne constitue pas un SaaS opéré** ;
- le retrieval sémantique reste optionnel et les résultats concernés restent `HEURISTIC` ;
- les observations runtime restent partielles et leur absence ne prouve jamais une absence d'exécution.

L'issue #98 conserve le suivi de l'isolation OS réelle Windows/Linux.

## Assets Windows attendus

La GitHub Release stable `v1.0.0` doit publier :

```text
MINOS-1.0.0-windows-x64-setup.exe
MINOS-1.0.0-windows-x64-setup.exe.sha256
minos-1.0.0-windows-x64.zip
minos-1.0.0-windows-x64.zip.sha256
minos-1.0.0.cdx.json
minos-1.0.0.cdx.json.sha256
MINOS-1.0.0-THIRD-PARTY-NOTICES.txt
MINOS-1.0.0-THIRD-PARTY-NOTICES.txt.sha256
```

## Publication

La publication est assurée par le script autoritatif :

```text
scripts/release/publish-windows-release.ps1
```

avec :

```text
Version      = 1.0.0
TargetCommit = SHA exact de main
```

Le script construit avec Maven CI-friendly via `-Drevision=1.0.0`, valide les artefacts, effectue les smoke tests setup/ZIP, vérifie la désinstallation, puis crée `v1.0.0` et la GitHub Release uniquement si tous les gates de publication passent.
