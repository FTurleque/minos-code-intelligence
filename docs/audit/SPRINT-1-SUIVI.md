# Sprint 1 — suivi de la remédiation (audit 2026-09)

> Branche : `sprint1/audit-remediation` (depuis `develop`, base `3ec9dc9c`).
> Constats : S1, S2, S3, S4, Q1, G2 — voir `AUDIT-2026-09.md`.
> Tenu à jour par l'agent `verif-qualite` et l'orchestrateur. Aucun push, aucune CI.

## 1. Tableau de bord

| Constat | Agent | Worktree / branche | Statut | Test rouge | Test vert | Commit |
|---|---|---|---|---|---|---|
| S1 | impl-hosted | `sprint1/impl-hosted` | ✅ terminé (fusionné, vérifié, build vert) | `HostedRoleGovernanceTest` 8 tests, 5 échecs (`target\s1-red.log`) | 8/8 + suites hosted existantes vertes (`target\s1-green.log`) | `18c71559` → `47815f21` |
| S2 | impl-hosted | `sprint1/impl-hosted` | ✅ terminé (fusionné, V13/V15 corrigés, build vert) | `HostedDenialSaturationTest` 6 tests, 4 échecs + 1 erreur « hard capacity reached » (`target\s2-red.log`) | 37/37 verts sur les suites hosted (`target\s2-green.log`) ; correction : `HostedUnchainedRefusalTraceTest` 2/2 échecs → 39/39 verts (`target\s2b-*.log`) | `f622060d` → `86b68c7b`, `85bc723d` → `151bc112` |
| S3 | impl-runtime | `sprint1/impl-runtime` | ✅ terminé (fusionné, V5–V9/V17 OK, build vert ; tests Linux exécutés sur WSL) | `CgroupJobOwnershipTest` 11 tests, 6 échecs ; `LinuxCgroupStaleRecoveryTest` 7 tests, 3 échecs (`target
ed.log`) | Windows : 12/7 verts, Linux skippés ; WSL cgroup v2 réel : `LinuxCgroupJobOwnershipIsolationTest` 3/3 verts (`target\green.log`) | `cda5388f` → `d6661b1e` |
| S4 | impl-surface | `sprint1/impl-surface` | ✅ terminé (fusionné, vérifié, build vert) | `StorageBackendConfigurationTest` 1 échec, `HostedControlPlaneServiceTest` 4, `LocalMinosTeamApiTest` 3 (`target/s4-red*.log`) | 13/10/5 verts (`target/s4-green.log`) | `076443e4` → `6377c1bc`, `c16acf80` → `e5b336b5` |
| Q1 | impl-surface | `sprint1/impl-surface` | ✅ terminé (fusionné, E1–E7/V16 OK, build vert) | `TeamCommandTest` 5 tests, 3 échecs (`target/q1-red2.log`) | 6/6 + suite minos-cli 78/78 (`target/q1-*2.log`) | `f6d35ebb` → `fe0deefc` |
| G2 | impl-docs | `sprint1/impl-docs` | ✅ terminé (fusionné, chaque phrase relue contre le code) | n/a | n/a | `3824551c` → `39fb40b7` |

## 2. Constats de la vérification (verif-qualite / verif-build)

| # | Constat | Fichier:ligne | Sévérité | Ce qui casse | Résolution |
|---|---|---|---|---|---|
| V1 | S1 (impl-hosted) | `minos-application/src/main/java/com/minos/hosted/HostedMembershipService.java:50` et `HostedTokenService.java:48` | remarque | Correctif relu contre les attaques A1–A13 (ADMIN→OWNER sur soi/autrui, rétrogradation ou révocation d'un OWNER, `issue` au nom d'un OWNER) : tous refusés via `HostedRole.canGovern` (sous-ensemble de permissions, seul OWNER gouverne OWNER ; AUDITOR/CONTRIBUTOR non comparables restent gouvernés par ADMIN). Refus audité DENIED avant toute écriture ALLOWED, message sans détail interne. Rien de bloquant. Exigence : preuve du rouge (sortie Maven de `HostedRoleGovernanceTest` sur le code de base) à joindre au commit, le diff non commité ne la montre pas. | en attente commit |
| V2 | S1/S2 (impl-hosted) | `HostedAuthorizationService.java:71-84` (`deny`/`recordDenial`) | à corriger (dans S2) | Le nouveau chemin `deny()` persiste un DENIED avec `version+1` exactement comme le refus de permission : le mécanisme S2 (limitation/agrégation/réserve) DOIT couvrir aussi ce chemin, sinon un ADMIN en boucle sur `grant(x, OWNER)` sature l'audit (scénario B8). | à vérifier au diff S2 |
| V3 | G2 (impl-docs) | `README.md:192`, `docs/user/cli.md:338`, `docs/STATUS.md:13`, `docs/ROADMAP.md:35`, `docs/user/production-installation.md:5-7` | remarque | Chaque phrase relue contre le code : `SUPERVISED_HARD_KILL` (`LinuxBubblewrapWorkerSandboxBackend.java:160-164`, `WindowsAppContainerWorkerSandboxBackend.java:187-191`), rétrogradation `UNTRUSTED_CODE_UNSUPPORTED` (`WorkerSandboxQualification.java:41-51`), test `currentOsBackendsFailClosedUntilStorageIsOsEnforced`, `supportsManagedLocalProvider` (`WorkerSandboxBackend.java:59`), `docs/user/remote-indexing.md` existe, ADR 0040 présent, Coursier bien utilisé par `ManagedScipProviderRuntimeManager`. Exact. `docs/ROADMAP.md` et `docs/STATUS.md` sont dans la zone `docs/**`. Commit `3824551c` revérifié : `LocalIsolatedIndexWorker.execute:117-121` lève bien avant tout provider distant si `!supportsUntrustedCode()` (ALLOW comme DENY), `WorkerSandboxBackends.strongestAvailable` filtre sur `supportsUntrustedCode` (→ natif) alors que `strongestAvailableForManagedLocalProvider` filtre sur `supportsManagedLocalProvider` (→ sandbox utilisée en local), `unmetRequirements()` exige `FILESYSTEM_WRITE_BYTES/ENTRIES` OS-enforced ; `ManagedScipProviderRuntimeManager` télécharge Coursier et Node.js (Windows) et exige `npm` ; aucun ADR modifié ; aucune `<version>` du POM modifiée. | ok (commit 3824551c) |
| V4 | G2 (impl-docs) | `minos-storage-postgresql/pom.xml:100-101` | remarque | Le commentaire corrigé ne cite plus aucune version (« see their <version> elements above ») : vrai mais moins utile ; le module mélange `testcontainers` 2.0.5 et `junit-jupiter`/`postgresql` 1.21.4 (Q17). Citer explicitement « Testcontainers 2.0.5 » serait plus précis. Ligne 101 dépasse la largeur des autres lignes du bloc (reformatage mineur). | facultatif |
| V5 | S3 (impl-runtime) | `minos-runtime-local/src/main/java/com/minos/runtime/CgroupJobOwnership.java:34` | bloquant (compilation) | Regex `MARKED_NAME` écrite `\.own-` dans un littéral Java : `\.` est un échappement illégal → `javac` refuse la classe, tout `minos-runtime-local` ne compile plus. Il faut `\\.own-`. (Diff non commité, phase test rouge ; verif-build le verra dès le premier build.) | **résolu** (ronde 3 : `\.own-` dans le diff non commité) |
| V6 | S3 (impl-runtime) | `CgroupJobOwnership.Mark.suffix()` + `LinuxCgroupJob.SAFE_JOB_NAME` (`{0,95}`, 96 chars max) + `LinuxBubblewrapWorkerSandboxBackend.java:191` | à corriger | Nom bwrap actuel `minos-<UUID 36>-<nanoTime hex ≤16>` = jusqu'à 59 chars ; suffixe `.own-<pid ≤10>-<epochMs 13>-<8 hex>` = jusqu'à 38 → 97 > 96 : `jobDirectory` lèvera « cgroup job name is not a safe single path segment » sur les runs sandbox Linux (le probe et `minos-provider-<UUID>` = 89 passent). Prouver la borne par un test (nom marqué maximal accepté) ou élargir `SAFE_JOB_NAME` en justifiant. | **résolu par conception** (ronde 3 : `jobDirectory` valide le nom de base ≤ 96 puis `markedJobName` ajoute le suffixe ≤ 38 → ≤ 134 < NAME_MAX 255 ; le nom marqué n'est plus soumis à `SAFE_JOB_NAME`). Reste facultatif : un test qui crée un job avec un nom de base de 96 chars. **Résolu** commit `cda5388f` : `MAX_SUFFIX_LENGTH` = 44 et test `theLongestSafeJobNameStillFitsWithItsMark` (nom de base 96 + suffixe ≤ 44). |
| V7 | S3 (impl-runtime) | `CgroupJobOwnershipTest.anUnmarkedCgroupIsReclaimedOnlyWhenItHoldsNoProcess`, `LinuxCgroupStaleRecoveryTest.anUnmarkedCgroupWithLiveProcessesIsLeftIntact`, ancien `liveStaleCgroupWithoutKernelKillControlFailsClosed` renommé | à corriger (justifier + journaliser) | Changement de contrat : avant, un cgroup `minos-*` non marqué encore peuplé = échec de confinement qui **rejetait la racine déléguée** (fail-closed, Javadoc de `reclaimStaleJobs`) ; après, il est laissé en place **et la racine est acceptée**. Bon compromis (ne jamais tuer un propriétaire inconnu) mais il doit être (1) explicite dans le message de commit et la Javadoc, (2) journalisé en WARNING avec `Verdict.reason` (sans chemin absolu), (3) couvert par un test prouvant que la racine reste utilisable et que le résidu est signalé. Sinon un résidu peuplé devient invisible. | **en grande partie résolu** (ronde 3 : Javadoc de `reclaimStaleJobs` réécrite, WARNING « MINOS leaves cgroup <nom> intact: <raison> » sans chemin, `StaleSweep(reclaimed, leftIntact)` retourné). Reste : un test qui asserte `leftIntact` contient le résidu non marqué peuplé (et que `qualifyRoot` accepte la racine), et la mention explicite du changement de contrat dans le message de commit. **Résolu** commit `cda5388f` : changement de contrat annoncé explicitement dans le message, test `anUnmarkedCgroupWithLiveProcessesIsLeftIntact` asserte `leftIntact`. |
| V8 | S3 (impl-runtime) | `LinuxCgroupJob.probe` (`minos-probe-<uuid>`, ligne 204) | à corriger | Le probe n'est pas marqué : deux JVM démarrant simultanément → la sweep de l'une voit le probe vide et non marqué de l'autre (verdict RECLAIM « non marqué + 0 processus ») et le supprime → `job.close()` du probe échoue (`unable to reclaim empty cgroup`) → racine rejetée pour cette JVM, sans sandbox. Marquer le probe avec `CURRENT` (scénarios C4/C11). | **résolu** (ronde 3 : `probe` utilise `CURRENT.markedName(...)`) |
| V9 | S3 (impl-runtime) | `CgroupJobOwnership.decide` (stub) ; `CgroupJobOwnershipTest`, `LinuxCgroupJobOwnershipIsolationTest` | remarque (conforme) | Conception relue contre C1–C11 : marque dans le nom (cgroupfs refuse les fichiers arbitraires : C3 ok), PID + instant de démarrage avec tolérance 2 s (PID réutilisé : C1 ok), instant inconnu → LEAVE (jamais tuer un vivant non vérifiable), cgroup vide d'un processus vivant marqué → LEAVE sans lire `cgroup.procs` (C4 ok), `Locale.ROOT` (C5 ok), `assumeTrue` sur la racine déléguée + `delegatedRoot()` vide hors Linux (C6 ok), `LinuxCgroupStaleRecoveryTest` reste portable Windows (`ProcessHandle` seulement). Tests rouges par construction (stub RECLAIM). Preuve du rouge à joindre au commit. | ok — `decide` implémenté en ronde 3, conforme aux tests (token propre → LEAVE sans lookup ; PID absent → RECLAIM ; instant absent d'un côté → LEAVE ; dérive > 2 s → RECLAIM). |
| V10 | S4 (impl-surface) | commit `076443e4` : `minos-application/src/main/java/com/minos/hosted/HostedControlPlaneService.java`, `HostedTenantService.java`, `HostedTokenService.java`, `src/test/.../HostedControlPlaneServiceTest.java` | à corriger (périmètre) | Ces 4 fichiers sont dans la zone `hosted/**` d'impl-hosted, à qui l'orchestrateur a confié l'élargissement S4 hosted ; `HostedTokenService.java` est déjà modifié par `18c71559` (hunks distincts, fusion probablement automatique, mais double travail garanti). Retirer ces fichiers du commit d'impl-surface, ou confirmer qu'impl-hosted ne les touche pas. Les DTO `MinosTeamApi` (`minos-api`) sont acceptés par décision de l'orchestrateur. | **résolu** (orchestrateur : impl-hosted ne touche plus ces records ; élargissement d'impl-surface conservé) |
| V11 | S4 (impl-surface) | `StorageBackendConfiguration.java:119-130` ; `StorageBackendConfigurationTest.java:69` | remarque | Correctif conforme D1–D3 : mot de passe `***`, URL passée par `safePostgresUrl` (user-info et query supprimés, test avec `token=url-secret`), `equals`/`hashCode` inchangés, accesseur intact, message d'exception testé, test rouge avant (toString par défaut d'un record). Deux remarques : (a) `toString()` imprime `home=` (chemin absolu de `MINOS_HOME`) — pas une régression (`safeDescription` le fait déjà) mais à garder en tête pour S10 ; (b) le message d'échec `"toString leaks the password: " + value` imprimerait la fuite dans la sortie de test en cas d'échec — préférer un message sans `value`. | (b) **résolu** commit `c16acf80` ; (a) accepté |
| V12 | Q1 (impl-surface) | `minos-cli/src/main/java/com/minos/cli/TeamCommand.java` | — | Pas encore commencé aux rondes 2 et 3 (aucun diff CLI). Attaques E1–E7 prêtes. | en attente |
| V13 | S2 (impl-hosted) | `minos-application/src/main/java/com/minos/hosted/HostedAuditSink.java:11-21` (`publishUnchained` avec `default → publish(event)`), `HostedAuditDelivery.publishAfterCommit` | à corriger | Un refus non chaîné est livré par défaut au sink par le même `publish` qu'un événement commité : le sink externe ne peut pas les distinguer, or l'événement non chaîné porte la séquence `auditSequence+1` et `previousHash` = tête de chaîne actuelle, séquence qui sera réutilisée par le prochain événement commité. Scénario : sink externe indexé par (tenant, sequence) → collision / apparence de fork de chaîne HMAC ; un vérificateur externe rejouant la chaîne conclut à une altération. Le message de `publishAfterCommit` (« event remains replayable from the tenant audit chain ») devient faux pour ces événements. Correctif : pas de `default` qui aliasse `publish` (le seul implémenteur du dépôt est `embeddedNoop`), ou un `default` qui journalise WARNING sans appeler `publish`, et une livraison distincte (`publishUnchained` sans passer par `publishAfterCommit`, ou message de log dédié). | **non résolu dans `f622060d`** (le message assume le `default → publish` comme « compromis documenté ») ; commit de correction attendu (ronde 4 : aucun diff non commité) Ronde 5, diff non commité relu : `default publishUnchained` n'appelle plus `publish` (journalise WARNING sur le logger `HostedAuditSink`), `embeddedNoop` devient une classe anonyme qui neutralise les deux méthodes (pas de double WARNING en production), Javadoc corrigée (« must never travel through publish »). **Résolu dans le diff**, commit attendu. **Résolu** commit `85bc723d`. |
| V14 | S2 (impl-hosted) | `HostedDenialSaturationTest` (6 tests), `HostedControlPlaneTestSupport.anotherProcess()` | remarque (conforme, impl en attente) | Conception lue dans les tests : budget par principal (10 refus chaînés / fenêtre 60 s, en mémoire par processus) + réserve persistée (refus chaînés plafonnés à 90 % de `retentionPolicy.maxAuditEvents`, donc multi-processus couvert). Couvre B1, B2 (budget par principal, pas par action/ressource), B3 (réserve côté état), B5 (aucun événement modifié), B8 (V2 : refus de gouvernance S1 partagent le budget), B11 (refus isolé chaîné), B12 (lectures non touchées). Tests rouges par construction (`publishUnchained` jamais appelé sur la base). Implémentation relue (ronde 3, non commitée) : `HostedDenialThrottle` `synchronized`, LRU bornée à 1 024 principaux × 10 horodatages (test concurrent 8 threads → 10 exactement), `deniedAuditCapacity() = max − max/10` sur `retentionPolicy.maxAuditEvents`, refus non chaîné → état et `version` intacts (B10 ✓), les deux chemins (`authorizeMutation` et `deny`) passent par `recordDenial` (V2 ✓). Remarques non bloquantes : (a) le slot du budget est consommé avant `HostedCommitRecovery.save` — un conflit de version optimiste consomme un slot sans chaîner (sens sûr) ; (b) un tenant dont la chaîne reste entre 90 % et 100 % de `maxAuditEvents` (la rétention par nombre ne retire qu'au-delà de 100 %) ne chaîne plus AUCUN refus tant que la rétention par âge n'a pas joué : à documenter dans le commit. Preuve du rouge à joindre. | conforme, en attente commit Commit `f622060d` relu (ronde 4) : B1–B12 rejoués sur la version commitée, identiques à la relecture (B1 ✓, B2 ✓, B3 ✓ réserve, B4 ✓ LRU, B5 ✓, B7 ✓, B8/V2 ✓, B10 ✓, B11 ✓, B12 ✓ ; B6/B9 = V15/V13 ouverts). V14b (90–100 %) non documenté dans le message ; preuve du rouge non mentionnée. Périmètre : 10 fichiers tous `hosted/**`, `--ignore-all-space` identique, check-module-boundaries SUCCESS (sources=473). V14b **documenté** dans `85bc723d`. |
| V15 | S2 (impl-hosted) | `HostedAuthorizationService.recordDenial` (branche `!chained`) + `HostedControlPlaneService` constructeur public 5 args → `HostedAuditSink.embeddedNoop()` ; `MinosApplicationAssembler.java:153` (production, CLI `team`) | à corriger | En production (CLI/app), le sink est `embeddedNoop` : un refus au-delà du budget (11e refus/min d'un principal, ou chaîne ≥ 90 % de `maxAuditEvents`) est livré à un no-op et **ne laisse aucune trace** — ni chaîne, ni sink, ni log (`HostedAuditDelivery.publishUnchained` ne journalise qu'en cas d'IOException). Scénario B6 : un VIEWER boucle sur `workspace-create` ; les 10 premiers refus sont chaînés, tous les suivants disparaissent silencieusement. Le brief exige que « la traçabilité d'un refus reste réelle ». Correctif minimal : journaliser chaque refus non chaîné (WARNING, tenant/principal/action/requestId, sans chemin ni jeton) dans `HostedAuditDelivery.publishUnchained` avant l'appel au sink, et le dire dans la Javadoc de `embeddedNoop`. | **non résolu dans `f622060d`** ; commit de correction attendu (WARNING par refus non chaîné + test) Ronde 5, diff non commité relu : `HostedAuditDelivery.publishUnchained` journalise WARNING (tenant, principal, action, resourceType, requestId ; aucun jeton) AVANT tout appel au sink ; Javadoc d'`embeddedNoop` le dit ; test `HostedUnchainedRefusalTraceTest` (2 cas : sink de production → 1 WARNING ; sink lambda legacy → jamais via `publish`, séquences distinctes, 1 WARNING sink). **Résolu dans le diff**, commit attendu (message : V14b à documenter). Remarque : avec un sink externe non adapté, deux WARNING par refus non chaîné (delivery + default du sink) — acceptable. **Résolu** commit `85bc723d` (message : V13, V15 et V14b documentés ; 3 fichiers `hosted/**` ; `--ignore-all-space` identique ; check-module-boundaries SUCCESS 473 ; arbre propre). |
| V17 | S3 (impl-runtime) | commit `cda5388f` | remarque (conforme) | Message : identifiant S3, conception justifiée, table de décision, changement de contrat V7 annoncé, borne V6 annoncée (44) et testée, `assumeTrue` cité. Périmètre : 6 fichiers tous `minos-runtime-local/src/**` ; `--ignore-all-space` identique. V5 corrigé (`\.own-`), V8 corrigé (probe marqué). check-module-boundaries SUCCESS (sources=473). Preuve du rouge non mentionnée dans le message (à confirmer par le rapport de l'agent). Aucun diff non commité restant. | ok |
| V18 | fusion (orchestrateur) | `sprint1/audit-remediation` HEAD `d6661b1e` (8 commits rebasés en avance rapide sur `647507c5`) | remarque (conforme) | (1) `git diff --stat e81cadc3..d6661b1e` : 39 fichiers, tous classés — G2 : README, docs/ROADMAP, docs/STATUS, docs/user/{README,cli,installation,production-installation}, commentaire `minos-storage-postgresql/pom.xml` (re-passé à la ligne, aucune `<version>`) ; S1/S2 : `hosted/**` application (9 main, 6 test) + domain (`HostedRole`, `HostedRetentionPolicy`, `HostedModelTest`) ; S3 : `minos-runtime-local/src/**` (2 main, 4 test) ; S4 : `storage/**` (main + test), records hosted (`HostedControlPlaneService`, `HostedTenantService`, `HostedTokenService`, `HostedControlPlaneServiceTest`) et `minos-api` `MinosTeamApi` + `LocalMinosTeamApiTest` (hors zone, acceptés) ; Q1 : `minos-cli` `TeamCommand` + test ; orchestrateur : `docs/audit/SPRINT-1-SUIVI.md`. Aucun fichier hors de toutes les zones. `--ignore-all-space` 2 074/96 vs 2 098/120 = réindentations déjà relues. (2) `HostedTokenService.java` fusionné : `canGovern` dans `issue()` (l.48) ET `toString()` de `Rotation` (l.127) ; `HostedControlPlaneServiceTest` : 4 tests S4 présents. (3) `patch-id --stable` identique pour les 8 paires (3824551c→39fb40b7, 18c71559→47815f21, f622060d→86b68c7b, 85bc723d→151bc112, 076443e4→6377c1bc, c16acf80→e5b336b5, f6d35ebb→fe0deefc, cda5388f→d6661b1e). (4) grep `N:\`, `C:\`, `/mnt/`, `/home/`, `password=`, `bearer`, `token=` sur les lignes ajoutées de `src/main` : seules occurrences = les masques `***` des `toString()` S4 ; aucun chemin absolu (main ni test). check-module-boundaries : SUCCESS (474, exit 0). Arbre propre. | ok |
| V16 | Q1 (impl-surface) | `minos-cli/src/test/java/com/minos/cli/TeamCommandTest.java` (3 tests, `SpyStore`) ; `TeamCommand.java` pas encore modifié | remarque (tests conformes E1, E2, E3 partiel) | `everyOperationRejectsUnknownOptionsBeforeReachingTheService` couvre les 17 opérations avec service espionné (E1 ✓, rouge sur la base car `service.tenant()`/`bootstrap` sont appelés avant `rejectUnknown`). `serviceFailuresExitWithExecutionCodeNotUsageCode` : IAE du service → 1, SecurityException → 1, jeton manquant → 1, **IOException → 1 avec message** (aujourd'hui `run` propage l'IOException : à intercepter dans `TeamCommand` via `CliCommandSupport.failureMessage`, sans chemin absolu). Points à surveiller à l'implémentation : (a) pour distinguer IAE d'usage et IAE de service, seul `UsageException` doit donner exit 2 — donc une IAE de validation levée par le domaine (`HostedPrincipal.safeId` sur `--principal "a b"`, `HostedRetentionPolicy` hors bornes construite dans la CLI) passera à exit 1 : changement de code de sortie à documenter dans le commit (E2) ; (b) aucun test ne verrouille encore les JSON `{"status":"REVOKED"}`, `BOUND`, `UNBOUND` (E3) ; (c) `MinosCli.java` ne doit pas être touché (E7). | en attente impl `TeamCommand.java` relu non commité (ronde 4) : E1 ✓ (chaque opération, `bootstrap` compris, consomme toutes ses options puis `rejectUnknown` avant le premier appel de service), E2 ✓ (`UsageException` → 2 ; IAE/Security/IllegalState/IOException du service → 1 ; bornes `HostedRetentionPolicy` re-levées en `UsageException` → 2 conservé), E3 ✓ (JSON `REVOKED`/`BOUND`/`UNBOUND` et renderers inchangés), E4 ✓, E5 ✓ (`safeMessage`), E6 : option inconnue (2) prime désormais sur jeton manquant (1), acceptable ; E7 ✓ (`MinosCli.java` intouché ; `--ignore-all-space` 276 vs 293 lignes = réindentation du `switch`). Restes : (a) IAE de validation domaine (`safeId` sur `--principal "a b"`, `audit --limit 0`) passent de 2 à 1 — à annoncer dans le message de commit ; (b) toujours aucun test verrouillant les JSON ; (d) `noOptions()` devenu code mort. Ronde 5, diff non commité : (b) **résolu** — `existingJsonOutputsAreUnchanged` verrouille `REVOKED`, `BOUND` (projectId + snapshotId), `UNBOUND` et les renderers ; (d) **résolu** — `noOptions()` supprimé ; (a) en attente du message de commit. (a) **Résolu** commit `f6d35ebb` : le message annonce le changement de code de sortie (IAE domaine/service 2 → 1, options validées avant la lecture de `MINOS_TEAM_TOKEN`), cite les 4 tests rouges→verts. Périmètre : 2 fichiers, `minos-cli/src/**` uniquement ; `--ignore-all-space` 329/54 vs 346/71 = réindentation du `switch` extrait dans `execute()` ; check-module-boundaries SUCCESS (472) ; arbre propre. **Q1 conforme.** |

## 3. À traiter plus tard (hors périmètre, relevé pendant le sprint)

| Origine | Description | Renvoi |
|---|---|---|
| impl-docs (G2) | `scripts/release/build-windows-distribution.ps1:359` : le README.txt généré dans le ZIP présente encore `tools install scip-java` en « Quick start » | D1 / ADR 0040 |
| impl-docs (G2) | `minos-storage-postgresql/pom.xml:56-72` : versions testcontainers mélangées (2.0.5 vs 1.21.4) | Q17 |
| impl-docs (G2) | `docs/architecture/arc42/06-vue-execution.md:75`, `docs/roadmap/M14_EXECUTION.md` : `tools install` comme flux nominal/historique, non modifiés | D1 |
| impl-surface (S4) | `minos-application/.../output/DeterministicJson.java:40` : repli `quote(value.toString())` pour tout objet non reconnu — piège pour tout futur record porteur de secret | Q7 / S10 |
| impl-surface (S4) | `minos-application/.../storage/MinosRuntimeSettings.java:39-42` : `environment`/`Properties` bruts conservés ; sûr sans `toString`, fragile à toute concaténation future | S4 (suite) |
| impl-hosted (S1) | `HostedAuthorizationService.authorizeRead:33-45` : un refus de lecture n'est pas audité, contrairement aux refus de mutation | S2 (suite) |
| impl-hosted (S1) | `HostedAuditDelivery.publishAfterCommit:31` : le log WARNING inclut l'UUID du tenant | politique de journalisation |
| impl-runtime (S3) | `LinuxCgroupJob.relocateSelf:147-156` : déplace tout le processus dans `<root>/minos-controller`, partagé par toutes les instances | S3 (suite) |
| impl-runtime (S3) | `LinuxCgroupJob.java:199,222` : messages existants journalisant des chemins absolus (`child`, `probe`) | S10 |
| impl-hosted (S2) | `HostedRetentionService.plan:87-89` : `maxAuditEvents` n'est qu'une cible de rétention, jamais une borne d'écriture pour les mutations autorisées — décision produit | ADR à venir |
| impl-hosted (S2) | `HostedAuditChain.verify` sur 100 000 événements ≈ 0,2 s par `loadVerified` | A6 |
| impl-hosted (S2) | tout futur sink d'audit durable hors `hosted/` devra redéfinir `publishUnchained` | S2 (suite) |
| impl-surface (Q1) | `TeamCommand.java` : `project-bind`/`member-revoke`/`project-unbind` construisent leur JSON à la main (`jsonEscape`) au lieu de `DeterministicJson` | Q7 / Q13 |
| impl-surface (Q1) | parsing d'options propre à `TeamCommand.parseOptions`, non unifié avec les autres commandes | Q11 |
| impl-docs (G2) | `docs/generated/product-facts.md:60` : fichier généré mentionnant `tools install` | D1 |
| verif-qualite (S4) | Records portant un jeton hors zone `impl-surface` : `HostedControlPlaneService.BootstrapResult`/`RotateKeyResult`, `HostedTenantService.Bootstrap`, `HostedTokenService.Rotation` (zone impl-hosted) et `MinosTeamApi.BootstrapDto/TokenDto/RotationDto` (module `minos-api`, hors toutes zones) : leur `toString()` par défaut imprime le bearer token. | S4 élargi ou sprint 2 |
| verif-qualite (S1) | `HostedMembershipService.revoke` distingue « membre inexistant » (IllegalArgumentException → exit 2 CLI) de « OWNER non gouvernable » (SecurityException → exit 1) ; pas une fuite (MEMBER_WRITE implique MEMBER_READ) mais incohérent avec `grant` qui refuse uniformément. | Q11 |
| verif-qualite (S3) | `LinuxCgroupStaleRecoveryTest` s'exécute sans garde OS sur un `@TempDir` : toute marque d'appartenance S3 lue depuis `/proc` ou `ProcessHandle` doit rester portable Windows dans ce test. | impl-runtime |

## 4. Journal

- 2026-09-26 — ouverture du sprint, création des worktrees.
- 2026-09-26 — impl-docs : G2 commité (`3824551c`), transmis à verif-qualite.
- 2026-09-26 — impl-hosted : S1 commité (`18c71559`), preuve rouge fournie ; V2 transmis comme exigence S2 ; élargissement S4 sur les records hosted confié à impl-hosted, DTO minos-api à impl-surface.
- 2026-09-26 — verif-qualite : ronde 1 terminée (V1–V4, G2 conforme), ronde 2 lancée.
- 2026-09-26 — impl-hosted : S2 commité (`f622060d`), preuve rouge fournie ; V13/V15 renvoyés pour un commit de correction.
- 2026-09-26 — impl-runtime : S3 commité (`cda5388f`) après résolution de V5–V8 ; tests Linux exécutés en vrai sur WSL (cgroup v2 délégué) en plus du skip propre sur Windows.
- 2026-09-26 — impl-surface : S4 commité (`076443e4`), élargissement records hosted + DTO minos-api ; l'instruction S4-hosted donnée à impl-hosted est annulée (doublon). Q1 lancé. Build croisé hosted+surface demandé à verif-build.
- 2026-09-26 — interruption (limite de session) pendant la correction S2, Q1, la ronde 4 et les builds ; les 4 agents ont été repris depuis leur transcript. S3 confirmé terminé (V7 satisfait dans le commit `cda5388f`).
- 2026-09-26 — impl-hosted : correction S2 commitée (`85bc723d`) : V13 (default du sink sans `publish`), V15 (WARNING par refus non chaîné, testé avec `embeddedNoop`), V14b documenté. Transmis à verif-build et verif-qualite.
- 2026-09-26 — verif-build : baseline verte (domain 24, engine 132, runtime-local 156 dont 24 skips Linux, storage-local 75, provider-scip 84, integration-git 27, application 300, nexus 10, cli 74 ; 0 échec, 0 flaky) ; S1, S4, S2 verts sans régression. Correction S2, S3 et croisé en cours.
- 2026-09-26 — impl-surface : Q1 commité (`f6d35ebb`), relu conforme (V16 a/b/c/d résolus). Aucun constat ouvert sur les 6 lots.
- 2026-09-26 — fusion : chaque branche `sprint1/impl-*` rebasée sur `sprint1/audit-remediation` puis avancée rapide (historique linéaire, hashes réécrits, voir tableau). Trois fichiers vides parasites (0 octet, créés à 01:21 dans le dépôt principal par un agent, mêmes noms que les nouveaux fichiers S3) ont été déplacés dans le scratchpad de session avant la fusion de S3. `check-module-boundaries.py` sur la branche fusionnée : SUCCESS, sources=474.
- 2026-09-26 — verif-qualite : relecture de fusion (V18) conforme — périmètre, `patch-id` identiques pour les 8 commits rebasés, aucun secret ni chemin absolu ajouté. Fin de mission, V1–V18 clos.
- 2026-09-26 — verif-build : build fusionné `d6661b1e` (`-pl minos-application,minos-api,minos-cli -am test`) VERT : 985 tests, 0 échec, 0 erreur, 42 skips (Linux-only) ; deltas vs baseline : domain +2, runtime-local +18 (+3 skips), application +24, api +3, cli +4 ; 0 test instable sur 7 builds. Logs sous `minos-wt-sprint1/verif-build/target/build-*.log`.
- 2026-09-26 — sprint clos côté agents. Reste : `clean verify` complet sur autorisation explicite de l'utilisateur. Rien poussé, aucune CI déclenchée.
- 2026-09-26 — PR #296 ouverte vers `develop` (sur demande). CI : 3 checks rouges, une seule cause : le gate `scripts/docs/product-facts.py:126` exigeait la phrase littérale « IMPLÉMENTÉE + QUALIFIÉE » de l'ancienne revendication #98 corrigée par G2. Gate aligné sur le fait exact (primitives implémentées, qualification code non fiable refusée) et l'ancienne formulation rejetée comme obsolète.

## 5. Bilan

| Constat | Fichiers de production | Tests ajoutés | Rouge → vert |
|---|---|---|---|
| S1 | `HostedRole.canGovern`, `HostedAuthorizationService.deny/recordDenial`, `HostedMembershipService.grant/revoke`, `HostedTokenService.issue` | `HostedRoleGovernanceTest` (8), `HostedModelTest` (+1), harnais `HostedControlPlaneTestSupport` | 5 échecs / 8 → 8/8 |
| S2 | `HostedRetentionPolicy.deniedAuditCapacity`, `HostedDenialThrottle` (nouveau), `HostedAuthorizationService.recordDenial`, `HostedAuditChain.event`, `HostedAuditSink.publishUnchained`, `HostedAuditDelivery.publishUnchained` | `HostedDenialSaturationTest` (6), `HostedDenialThrottleTest` (3), `HostedUnchainedRefusalTraceTest` (2), `HostedModelTest` (+1) | 4 échecs + 1 erreur « hard capacity reached » / 6 → 6/6 ; correction : 2/2 échecs → 2/2 |
| S3 | `CgroupJobOwnership` (nouveau), `LinuxCgroupJob.reclaimStaleJobs/markedJobName/probe` | `CgroupJobOwnershipTest` (12), `LinuxCgroupStaleRecoveryTest` (7, réécrit), `LinuxCgroupJobOwnershipIsolationTest` (3, Linux) | 6/11 + 3/7 échecs → 12/12, 7/7 ; 3/3 verts sur WSL cgroup v2 |
| S4 | `StorageBackendConfiguration.toString`, records `BootstrapResult`, `RotateKeyResult`, `Bootstrap`, `Rotation`, DTO `MinosTeamApi` | `StorageBackendConfigurationTest` (+1), `HostedControlPlaneServiceTest` (+4), `LocalMinosTeamApiTest` (+3) | 1 + 4 + 3 échecs → tous verts |
| Q1 | `TeamCommand.run/execute`, `retentionPolicy`, suppression de `noOptions` | `TeamCommandTest` (+4, espion `SpyStore`) | 3 échecs / 5 → 6/6, suite CLI 78/78 |
| G2 | `README.md`, `docs/STATUS.md`, `docs/ROADMAP.md`, `docs/user/{README,cli,installation,production-installation}.md`, commentaire `minos-storage-postgresql/pom.xml` | n/a (chaque phrase vérifiée contre le code) | n/a |
