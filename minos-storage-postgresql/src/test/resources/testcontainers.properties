# Keep Testcontainers deterministic without forcing a deprecated Docker client strategy.
# Windows CI receives DOCKER_HOST from the Windows-only Maven profile; Linux/macOS
# should use Testcontainers' native provider discovery (for example /var/run/docker.sock).
testcontainers.reuse.enable=false
