package com.minos.dynamic;

/** Kinds of partial runtime observations accepted by the M26 interchange contract. */
public enum RuntimeObservationType {
    SYMBOL_EXECUTION,
    CALL,
    LINE_COVERAGE
}
